<h1>Projects</h1>
<p><%= @projects.count %> projects</p>

<table>
  <tr>
    <th>Title</th>
  </tr>
  <% @projects.each do |project| %>
    <tr>
      <td><%= project.title %></td>
      <td><%= link_to 'Show', project %></td>
    </tr>
  <% end %>
</table>

